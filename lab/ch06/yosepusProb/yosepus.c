#include <stdio.h>
#include "LinkedQueue.h"

element yosepus(int n, int k);

int main(void)
{
    unsigned int n, k;

    printf("Input (n, k): ");
    scanf("%d %d", &n, &k);
    printf("\n마지막까지 살아남은 사람은 %d번째 사람입니다.\n", yosepus(n, k));

    return 0;
}

element yosepus(int n, int k)
{
    element item;

    // 이곳을 채우시오

    return item;
}
